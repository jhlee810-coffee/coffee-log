# Coffee Log — 핸드오프 문서
작성일: 2026-05-27

---

## 프로젝트 개요

- **경로 (Windows)**: `D:\BI_T1m\coffee-log\`
- **경로 (맥미니)**: `~/coffee-log/`
- **GitHub repo**: `https://github.com/jhlee810-coffee/coffee-log.git`
- **GitHub 계정**: `jhlee810-coffee`
- **최신 커밋**: `544cab1` — Add cupping system

### 배포/푸시 방법
```bash
# 파일 수정 후 SCP로 맥미니에 복사
scp "D:/BI_T1m/coffee-log/파일명" JHLee@192.168.0.5:~/coffee-log/

# SSH로 커밋 + 푸시
ssh JHLee@192.168.0.5 "cd ~/coffee-log \
  && git add 파일명 \
  && git commit -m '메시지' \
  && git remote set-url origin https://x-access-token:<PAT>@github.com/jhlee810-coffee/coffee-log.git \
  && git push origin main \
  && git remote set-url origin https://github.com/jhlee810-coffee/coffee-log.git"
```

### 로컬 미리보기
```bash
ssh JHLee@192.168.0.5 "cd ~/coffee-log && python3 -m http.server 8899 &"
# 브라우저: http://192.168.0.5:8899
```

---

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 전체 HTML 구조, 탭, 모달 |
| `style.css` | 모든 스타일 (CSS variables 포함) |
| `app.js` | DB 로드/저장, 탭 전환, 공통 헬퍼 |
| `data.js` | INITIAL_DATA (초기 생두/로스팅 샘플) |
| `dashboard.js` | 대시보드 렌더 |
| `beans.js` | 생두 목록, 상세, CRUD |
| `roasts.js` | 로스팅 로그, 상세, CRUD |
| `brewing.js` | 레시피 라이브러리, 브루잉 기록 |
| `wishlist.js` | 구매 검토 목록 |
| `cupping.js` | 커핑 시스템 (현재 미완성, 아래 참조) |
| `binbon_timer.html` | 빈본 타이머 (iframe으로 삽입) |

---

## DB 구조 (localStorage key: `coffee_v3`)

```javascript
{
  beans: [...],       // 생두 목록
  roasts: [...],      // 로스팅 로그
  recipes: [...],     // 브루잉 레시피
  brewlogs: [...],    // 브루잉 기록
  wishlist: [...],    // 구매 검토
  cuppings: [...]     // 커핑 세션 (추가됨)
}
```

### cupping 데이터 구조 (현재)
```javascript
{
  id: 'abc123',
  date: '2026-05-27',
  cups: [
    {
      cup_id: 'def456',
      bean_name: '에티오피아 예가체프',
      roast_id: 'roast_id_here',  // 로스팅 배치 연결
      scores: {
        fragrance: 7.5, aroma: 8.0, flavor: 7.5,
        aftertaste: 7.5, acidity: 8.0, body: 7.0,
        balance: 7.5, overall: 8.0
      },
      positives: ['자스민', '복숭아', '밝은산미'],
      negatives: [],
      notes: '메모',
      total: 60.0
    }
  ],
  session_notes: '전체 세션 메모'
}
```

---

## CSS 디자인 토큰

```css
--teal: #4ecdc4      /* 메인 포인트 */
--amber: #ffd166     /* 강조/점수 */
--coral: #ff6b6b     /* 경고/부정 */
--purple: #a78bfa    /* 커핑/레시피 */
--bg:  #1e252c       /* 페이지 배경 */
--bg2: #161b20       /* 내비/모달 배경 */
--bg3: #252d35       /* 카드 내 섹션 배경 */
--bg4: #1a2028       /* 더 어두운 박스 */
--text: #eef2f0      --text2: #a0b8c0
--muted: #4a6070     --muted2: #6a8898
--b1: #252d35        --b2: #2e3840
```

### 폰트
- `Bebas Neue` — 섹션 제목, 숫자 강조
- `DM Sans` — UI 전반 (기본체)
- `Josefin Sans` — 원두/생두명 전용
- `Playfair Display` — 이탤릭 메모/노트

---

## 현재 완료된 기능

- ✅ 생두 목록 CRUD
- ✅ 로스팅 로그 CRUD + DTR 자동 계산
- ✅ 로스팅 ↔ 브루잉 연계 (카드에서 상호 표시)
- ✅ 브루잉 기록 CRUD (물/가수 칩 선택, 분쇄도, 맛 슬라이더)
- ✅ 레시피 라이브러리 (공식 + 내 레시피, 단계별 빌더)
- ✅ 빈본 타이머 (iframe)
- ✅ 구매 검토 목록
- ✅ 커핑 탭/세션 저장/상세 모달 기본 틀 완성
- ✅ 커핑 ↔ 로스팅/생두 연계 (상세 모달에 커핑 히스토리 표시)

---

## 커핑 시스템 — 현재 상태와 문제점

### 현재 구현 (`cupping.js`)
- 세션 목록 렌더링 (`renderCupping`)
- 세션 저장/수정/삭제 (`saveCupping`, `openCuppingDetail`)
- 태그 선택 (긍정 103개, 부정 41개)
- 점수 칩 (6.0~10.0, 0.5 단위)
- 로스팅/생두 모달과 연계 (`cuppingMiniHtml`)

### 문제점 (유저 피드백)

#### 문제 1: 로스팅 구분 불가
같은 날짜에 같은 생두를 3가지 로스팅한 경우, 드롭다운에 표시되는 정보가 너무 적어 구분 불가.
- **현재**: `2026-05-25 [미디엄]`
- **필요**: `2026-05-25 [미디엄] 투입200g 배출170g DTR18.5%` 등 수치 추가 + 일련번호 포함

#### 문제 2: 컵 추가 UX 불명확
현재는 "+ 컵 추가" 버튼이 있지만 컵 2, 3, 4, 5를 각각 추가하는 흐름이 직관적이지 않음.
처음 폼 열면 컵1 하나만 보이는데, 이미 컵이 있는 상태에서 추가 버튼의 존재를 못 찾는 문제.

#### 문제 3: 세션 선택 화면에 점수표가 보임
커핑 탭 목록 카드(`cuppingCard`)에 점수(avgTotal)가 표시되는데, 아직 커핑을 시작하지 않은 경우에도 0점이 아닌 이상한 값이 나올 수 있음.
→ 점수는 세션 상세에서만 보여야 함 (목록에서는 태그 위주로).

#### 문제 4: 핵심 UX 흐름 미완성
현재 흐름: "새 세션" → 폼에서 생두+점수+태그 한 번에 입력
필요한 흐름:
1. **세션 설정**: 날짜 + 비교할 컵들 선택 (생두 + 로스팅 배치 지정)
2. **커핑 진행**: 단계별(fragrance → aroma → ... → overall) 순서대로 모든 컵 동시 평가
3. **마무리**: 태그 선택 + 메모 + 저장

즉, **세션 설정** → **단계별 평가 화면** 이 분리되어야 함.

---

## 커핑 시스템 — 권장 수정 방향

### 수정 1: 로스팅 드롭다운 표시 개선
`cupFormBlock()` 내 `roastOpts` 생성 부분 수정:
```javascript
// 현재
`${r.date}${r.roast_level?' ['+r.roast_level+']':''}`

// 개선 — 일련번호 + 핵심 수치 포함
const sortedAll = [...db.roasts].sort((a,b)=>a.date>b.date?1:-1);
const serial = sortedAll.findIndex(x=>x.id===r.id)+1;
const info = [
  `#${String(serial).padStart(2,'0')}`,
  r.date,
  r.roast_level?`[${r.roast_level}]`:'',
  r.input_g?`${r.input_g}g`:'',
  r.dtr_pct?`DTR${r.dtr_pct}%`:'',
].filter(Boolean).join(' ');
```

### 수정 2: UX 흐름 2단계 분리
**Step A — 세션 설정 (Setup 화면)**
- 날짜 선택
- 컵 목록: 처음부터 컵1~N이 나란히 보이는 카드 형태
- 각 컵에 생두 선택 + 로스팅 배치 선택
- 컵 추가/제거 버튼 명확하게
- "커핑 시작 →" 버튼

**Step B — 단계별 평가 (Scoring 화면)**
- 진행 바: fragrance → aroma → flavor → aftertaste → acidity → body → balance → overall
- 현재 단계 설명 (가이드 텍스트)
- 각 컵을 나란히 (또는 스크롤로) 표시하며 같은 단계를 동시에 평가
- 이전/다음 단계 버튼
- 마지막 단계 후: 태그 선택 + 메모 + 저장

### 수정 3: 목록 카드에서 점수 제거
`cuppingCard()` 에서 `avgTotal` 표시 제거, 대신:
- 비교한 생두 이름
- 가장 많이 선택된 긍정 태그 3~5개
- 날짜

---

## 커핑 태그 전체 목록

### 긍정 태그 (9 카테고리, 103개)
```
🌸 플로럴: 자스민, 장미향, 라벤더, 오렌지꽃, 인동꽃, 꽃향기전반, 허브향, 민트
🍑 과일(밝은): 복숭아, 살구, 자두, 체리, 블루베리, 딸기, 라즈베리, 크랜베리
🍋 시트러스/열대: 레몬, 라임, 오렌지, 자몽, 파인애플, 망고, 패션프루트, 멜론
🍷 숙성/건조과일: 포도, 사과, 배, 무화과, 말린과일향, 건포도, 말린자두, 말린체리, 말린살구, 잼같은, 과일사탕, 발효과일, 와인같은, 포트와인향
🍯 단맛/캐러멜: 캐러멜, 꿀, 황설탕, 시럽, 메이플, 바닐라, 버터스카치, 밀크초콜릿, 다크초콜릿, 코코아, 모카, 누가, 마지팬
🌰 견과류: 아몬드, 헤이즐넛, 땅콩, 호두, 피칸, 마카다미아
🌾 곡물/스파이시: 곡물향, 빵향, 토스트향, 시리얼향, 맥아향, 보리향, 쌀향, 계피, 정향, 카다몬, 생강, 흑후추
🪵 우드/어스시: 타바코향, 우드향, 삼나무, 가죽향, 흑차향, 얼그레이, 버섯향, 달콤한흙향
✨ 질감/산미/여운: 밝은산미, 부드러운산미, 산뜻한산미, 레몬산미, 사과산미, 복숭아산미, 와인산미, 시트러스산미, 달콤한산미, 크리미, 벨벳같은, 실키, 쥬시, 묵직한바디, 가벼운바디, 수분감있는, 둥근느낌, 매끄러운, 진한느낌, 뽀얀질감, 긴여운, 달콤한여운, 과일여운, 초콜릿여운, 꽃향여운, 깔끔한여운, 균형잡힌, 복합적인, 깔끔한, 투명한, 고급스러운
```

### 부정 태그 (5 카테고리, 41개)
```
🔥 로스팅 결함: 탄맛, 연기맛, 오버로스트, 언더로스트, 굽내, 그을린맛, 빵같은(열부족), 불균일로스팅, 쓴여운
🌿 생두/발효 결함: 풀냄새, 채소맛, 완두콩향, 과발효, 식초맛, 발효취, 리오이(요오드맛), 퀴퀴함, 곰팡이, 흙냄새, 습한흙
😣 떫음/쓴맛: 강한쓴맛, 떫음, 타닌느낌, 건조한수렴성, 텁텁함, 역함
⚗️ 오염/이취: 고무냄새, 약품맛, 금속맛, 화학약품향, 기름맛, 종이맛, 염소맛, 비누향, 양파냄새, 마늘냄새, 땀냄새
📉 밸런스 결함: 밋밋함, 너무묽음, 너무진함, 바디없음, 밸런스깨짐, 짧은여운, 텅빈느낌, 단조로운
```

---

## 커핑 8단계 가이드

| 단계 | 키 | 타이밍 | 설명 |
|---|---|---|---|
| 향(건식) | fragrance | 분쇄 직후 ~ 물 붓기 전 | 건식 향의 강도·질감 평가 |
| 향(습식) | aroma | 물 붓고 3~4분 후 | 크러스트 깨기 직전·직후 올라오는 향 |
| 맛 | flavor | 70°C 내외, 슬러핑 | 전체적인 맛의 첫인상 |
| 여운 | aftertaste | 삼킨 후 | 입안에 남는 향미 지속 시간과 질감 |
| 산미 | acidity | 맛 평가와 함께 | 강도보다 질(밝은지·와인같은지) |
| 바디 | body | 맛 평가와 함께 | 입안 무게감·질감 |
| 균형 | balance | 전체 평가 | 모든 요소가 어우러지는 정도 |
| 종합 | overall | 마무리 | 전반 인상·선호도 |

---

## 기타 중요 사항

- **로스팅 일련번호**: 날짜 오름차순 정렬 기준 `#01`, `#02`... (roasts.js의 `serialMap`)
- **BINBON_MODES**: `{1:460, 2:445, 3:397, 4:406, 5:470, 6:363, 7:406, 8:459}` (모드별 총 초, 2026-08-24 1·8번 추가 + 재계산)
- **DTR 공식**: `(1팝잔여초 - 배출잔여초) / (전체초 - 배출잔여초) * 100`
- **브루잉 ↔ 로스팅 연계 쿼리**:
  ```javascript
  db.brewlogs.filter(b =>
    b.roast_id === r.id ||
    (b.brew_type==='own' && b.bean_name===r.bean_name && !b.roast_id)
  )
  ```
- **모달 여닫기**: `openMo('id')` / `closeMo('id')`
- **저장**: `saveDB()` (localStorage에 즉시 저장)
- **ID 생성**: `genId()` (타임스탬프 + 랜덤)
