# 작업 로그 — 2026-08-24

## 세션 요약

이 세션에서 완료한 작업 목록. 다음 세션에서 이어받을 때 참고.

---

## 완료된 작업

### 1. 프로젝트 폴더 이동
**변경**: `D:\BI_T1m\coffee-log\` → **`D:\coffee-log\`**

- `D:\BI_T1m`은 자동매매 시스템 전용 폴더라서, 커피 로그를 최상위 폴더로 완전히 분리
- D 드라이브 전체 검색해서 다른 곳에 커피 로그 관련 파일 없는 것 확인함
- 경로 바뀐 곳 전부 갱신: `.claude/launch.json`(로컬 서버 경로), `COFFEE_LOG_STRUCTURE.md`(배포 절차), 자동 기억 파일들
- 맥미니(`~/coffee-log/`)와 GitHub 레포는 변경 없음 — 로컬(Windows) 경로만 이동

**검토했지만 기각한 안**: 맥미니를 거치지 않고 Windows에서 직접 git push하는 구조로 전환하는 것도 제안했으나, 사용자가 **현재 방식(Windows 수정 → 맥미니 경유 push) 유지**를 선택함. 이유: 맥미니가 자동매매 때문에 어차피 상시 가동 중이라 구조 변경 실익이 적음. **이후 세션에서 이 제안 다시 꺼내지 말 것.**

### 2. 탭 순서 변경
**파일**: `index.html`

대시보드 → 생두 목록 → **빈본 타이머** → 로스팅 로그 → 브루잉 → 커핑 → 구매 검토 순으로 변경 (기존엔 로스팅 로그 다음에 있었음).

### 3. 빈본 타이머 대개편 (메인 작업)
**파일**: `binbon_timer.html`, `roasts.js`, `index.html`, `app.js`

#### 배경
기존엔 모드 2~7번만 있었고, "원두 종류(워시드/내추럴/케냐)"별로 DTR을 선택하는 구조였음. 사용자가 실측 기초자료(모드 1~8번 전체의 DTR%·총시간·1차팝시간)를 제공하면서 전면 재설계 요청.

#### 설계 과정 (자세한 계산 근거는 `BINBON_MODES_BASE_DATA.md` 참고)
1. 사용자 제공 기초자료로 모드별 `자동배출초(ae)`, `1차팝 기준시간`, `center`(=ae−팝시간) 재계산 → 기존 코드값과 1~2초 차이나던 3·6·7번도 포함해 전부 재계산값으로 통일
2. Google Sheets 실측 로스팅 데이터(56건) 분석 → 1차팝 실제 편차가 최대 64초까지 나서, 여유폭을 **±30초 → ±60초**로 확대
3. "원두 종류" 개념을 완전히 없애고 **모드(1~8번) + DTR만으로 구성**하기로 결정
4. DTR 최대값 = `min((center+60)/ae×100, 35%)` — 사용자가 "6번 모드 DTR 24%로 로스팅해봤는데 다크하지 못했다"고 실측 피드백을 줘서 상한을 35%까지 열어둠
5. DTR 최소값은 모드가 오를수록 계단식 상향(다크 모드는 낮은 DTR 쓸 일 없으므로): 1~3번=10%, 4번=12%, 5번=14%, 6번=16%, 7번=18%, 8번=20% (이후 8번은 22%로 조정해 7번과 구분)
6. 선택지 개수를 모드당 10개 이하로 맞추기 위해, **라이트(1~3번)는 1% 간격 + 상한을 실용값으로 트림**, **미디움/다크(4~8번)는 2% 간격으로 35%까지** 하는 방식으로 최종 확정

#### 코드 반영
- `binbon_timer.html`: `MODES`(1~8번, 새 `ae`값), `MODE_DTRS`(모드별 그룹+DTR 리스트), `POP_ROWS`(±60초 반영한 새 기준표) 전면 교체
- **`BEAN` 객체(워시드/내추럴/케냐, color/dtrs/legend) 완전 삭제** — 그룹 색상은 라이트=그린(#0D6E54), 미디움=앰버(#C77A1F, 신규), 다크=브라운(#7A3B10)
- DTR별 상세 설명(legend) 텍스트는 대응 데이터가 없어져서 제거, 그룹명(라이트/미디움/다크)만 표시
- 기본 모드는 실사용 이력상 가장 많이 쓴 **3번**으로 설정
- `roasts.js`의 `BINBON_MODES` 상수 동일하게 갱신 (`brewing.js`/`cupping.js`는 이 상수를 값으로만 참조해서 자동 반영됨, 별도 수정 불필요)
- `index.html`의 수동 로스팅 기록 폼(`#modeBtns`)에도 1번·8번 버튼 추가
- `app.js`의 `binbon_timer.html?v=...` 캐시버스팅 버전 갱신

브라우저 실클릭 테스트로 모드 전환 시 색상/DTR리스트/자동배출시간 정상 반영 확인함.

### 4. 생두 선택 드롭다운 정렬
**파일**: `binbon_timer.html`

빈본 타이머의 "생두 선택" 드롭다운을 `<optgroup>`으로 **원산지별 그룹핑** + **그룹 내에서도 한글 오름차순 정렬**하도록 개선 (`localeCompare(..., 'ko')` 두 단계 적용). 실제 데이터로 검증: 과테말라→에콰도르→에티오피아→...→페루 순, 각 그룹 내부도 정렬 확인됨.

### 5. 배출량 기본값 + 0.1g 단위 조정
**파일**: `binbon_timer.html`

- 로스팅 실행화면의 최종 배출량 기본값을 **100g**으로 설정 (기존엔 빈 값)
- `adjOutput()`을 부동소수점 안전한 방식(`Math.round((cur+delta)*10)/10`)으로 바꿔서 0.1g 단위 조정 지원
- **버그 발견 및 수정**: 처음엔 −5/−1/−0.1/입력칸/+0.1/+1/+5를 한 줄에 다 넣었더니, 모바일(375px) 화면에서 입력칸이 **16px로 눌려서 숫자가 안 보이는** 버그 발생. 로직 자체는 정상 작동했지만 화면에 안 보여서 "안 된다"고 느껴진 것. → ±0.1 버튼을 별도 줄로 분리해서 해결 (입력칸 66px 확보 확인)

### 6. Git 배포 트러블슈팅 (중요 — 다음에 또 겪지 않도록 정리)

#### 겪었던 문제들
1. **맥미니 IP 변경**: `192.168.0.5` → `192.168.0.4` (기존 IP는 이 Windows PC가 점유 중이었음). `arp`/서브넷 스캔으로 확인.
2. **SSH 키 인증 실패**: Windows의 `id_ed25519` 개인키 파일에 불필요한 계정(`HOME\T_Agent`)의 접근권한이 남아있어서 OpenSSH가 키를 거부하고 비밀번호를 요구함. `icacls`로 소유자만 남기게 권한 재설정해서 해결.
3. **PAT 형식 문제**: GitHub PAT를 URL에 넣을 때 `TOKEN@host`나 `TOKEN:@host` 형식은 거부됨. **`x-access-token:TOKEN@host` 형식만 동작함.**
4. **토큰이 계속 죽는 문제**: 새로 발급한 토큰이 몇 번이고 바로 "Bad credentials"/"Invalid username or token"으로 실패. 처음엔 GitHub의 자동 보안 스캔이 채팅에 노출된 토큰을 즉시 폐기하는 걸로 추정했으나, **진짜 원인은 따로 있었음** (아래 5번).
5. **진짜 원인 — GitHub Push Protection**: `HANDOFF.md` 파일에 훨씬 예전에 문서화해뒀던 **진짜 PAT가 평문으로 박혀 있었음** (`ghp_jUrCqWut...`). 이 파일이 포함된 커밋을 push하려 할 때마다 GitHub가 "GH013: Repository rule violations — Push cannot contain secrets"로 막았던 것. 토큰 인증 자체는 문제 없었는데 에러 메시지가 인증 실패처럼 보여서 계속 새 토큰만 발급하며 헤맸음.
   - **해결**: `HANDOFF.md`에서 실제 토큰을 `<PAT>` 플레이스홀더로 교체 → 아직 원격에 push 안 된 로컬 커밋들을 `git reset --soft`로 되돌려서 깨끗한 커밋 하나로 재구성 → push 성공
6. **osxkeychain이 SSH 세션에서 안 먹힘**: 맥미니의 git `credential.helper=osxkeychain`은 GUI 로그인 세션에 묶여있어서 헤드리스 SSH 세션에서는 키체인 접근이 안 됨(빈 결과 반환 → git이 인터랙티브 프롬프트 시도 → tty 없어서 실패). **`credential.helper=store`로 변경**해서 해결 — 플레인텍스트로 `~/.git-credentials`에 캐시되므로 SSH에서도 정상 동작.

#### 최종 상태 (중요)
- 맥미니 git의 `credential.helper`는 이제 `store`. **PAT가 한 번 성공적으로 캐시된 이후로는, Claude가 토큰 없이 `ssh jhlee@<IP> "cd ~/coffee-log && git push origin main"` 만으로 직접 push 가능함이 확인됨.**
- 앞으로 push가 다시 인증 실패하면: (a) 캐시가 만료/삭제됐는지, (b) `HANDOFF.md`나 다른 문서에 실제 시크릿이 또 들어갔는지부터 확인할 것. **PAT를 채팅에 직접 붙여넣는 방식은 auto-mode 보안 필터가 Bash 실행을 차단하므로, 필요시 사용자가 직접 터미널에서 실행하되 저한테는 토큰 값을 공유하지 말라고 안내할 것.**
- 문서/메모에 실제 시크릿을 절대 평문으로 남기지 말 것 (플레이스홀더 사용) — 이번 사고의 근본 원인이었음.

---

### 7. 배치 번호 기능 삭제
**파일**: `binbon_timer.html`

더 이상 안 쓰는 기능이라 요청으로 삭제함 — `S.batchNum` 상태, "배치 번호" 선택 UI, `COOLING` 배열(강제 쿨링 권장 문구) 전부 제거. `sendRoastLog()`에 로그로 남는 값이 아니었어서 다른 파일에 영향 없음.

### 8. 페이지 좌우 스크롤 재발 방지
**파일**: `binbon_timer.html`

배출 후 로스트 로그 화면에서 좌우로 화면이 움직이는 버그 재발 신고. 로컬에서 재현은 안 됐지만(스크린샷 폭 375px 기준 overflow 없음), 근본 원인과 무관하게 막히도록 `html,body { overflow-x:hidden }`를 페이지 레벨에 추가. DTR 기준표(`.inline-table-wrap`)는 `overflow-x:auto`로 내부 스크롤은 그대로 유지됨 — 전체 표 모달(컬럼 최대 10개)로 테스트해서 내부 스크롤은 살아있고 페이지 스크롤은 없는 것 확인함.

### 9. 배치 번호 기능 삭제
**파일**: `binbon_timer.html`

더 이상 안 쓰는 기능이라 요청으로 삭제함 — `S.batchNum` 상태, "배치 번호" 선택 UI, `COOLING` 배열(강제 쿨링 권장 문구) 전부 제거. `sendRoastLog()`에 로그로 남는 값이 아니었어서 다른 파일에 영향 없음.

### 10. 로스팅 중 화면 꺼짐 방지 (Screen Wake Lock)
**파일**: `binbon_timer.html`

사용자가 "화면이 계속 켜져 있는데 중간에 어두워져서 다시 터치해야 한다"고 보고. `navigator.wakeLock.request('screen')`로 로스팅 시작(`doStart`) 시 화면 잠금 방지 걸고, 리셋(`handleReset`) 시 해제. iOS가 백그라운드/화면꺼짐 시 wake lock을 자동 해제하는 스펙이라, `visibilitychange` 이벤트로 화면이 다시 보일 때 로스팅 중이면 재요청하도록 처리함.

**참고**: 이 API는 iOS Safari 16.4+ 필요, 미지원 브라우저에선 조용히 무시됨(try/catch). 자동화 브라우저 도구에선 "페이지가 안 보인다"는 이유로 API 자체가 거부되어 실기기 테스트로 최종 확인 필요 — 코드는 표준 패턴이라 실제 폰에서는 정상 동작할 것으로 예상.

**추가로 짚어본 것**: 사용자가 "모바일에서 알람음이 안 들린다"고도 보고함. 원인 후보: (a) 아이폰 무음 스위치 — Web Audio API는 무음 스위치를 무시하지 않음, (b) 화면 꺼짐/백그라운드 시 iOS가 JS 타이머 자체를 정지시켜서 카운트다운·알람이 같이 멈춤 (이번 wake lock으로 상당 부분 해결될 것으로 기대), (c) 볼륨 자체가 30%로 작게 설정됨. **아직 실기기로 재확인 안 됨 — 다음 세션에서 확인 필요.**

### 11. 로스팅 상세보기에 경과시간/디벨롭타임 추가
**파일**: `app.js`(`fmtMS` 헬퍼), `roasts.js`(`openRoastDetail`)

"1팝 잔여"/"배출 잔여"만 있던 걸 "1팝 경과 (잔여 X:XX)" / "배출 경과 (잔여 X:XX)" / "디벨롭타임 XX초"로 확장. `pop_time`/`eject_time`이 "남은시간" 포맷으로 저장되는 걸 `BINBON_MODES[mode]`로 역산해서 경과시간 계산 (`ae - toSec(remaining)`), 디벨롭타임은 `toSec(pop)-toSec(eject)`로 계산(자동배출값 불필요). 실데이터로 검증: 모드3 로스팅 1건 기준 1팝경과 5:16(잔여1:21), 배출경과 6:04(잔여0:33), 디벨롭타임 48초 — 모두 수치 일치 확인.

### 12. "로스팅 기록" 폼 취소 버튼 동작 수정
**파일**: `index.html`(`roast_done` 메시지 리스너), `roasts.js`(`saveRoast`)

빈본 타이머에서 "로스트 로그 등록" 누르면 `roast_done` 메시지가 부모창으로 오는데, 기존엔 **폼을 여는 시점에 이미 로스팅 로그 탭으로 전환**해버려서 취소를 눌러도 저장을 눌러도 똑같이 로스팅 로그 탭이 보였음. 탭 전환을 폼 오픈 시점에서 **저장 성공 시점(`saveRoast()` 끝)**으로 옮겨서, 취소하면 원래 있던 화면(빈본 타이머)이 그대로 유지되도록 수정. 시뮬레이션 테스트로 확인: 취소 → 탭 유지, 저장 → 로스팅 로그 탭으로 전환 + 모달 닫힘.

### 13. "로스팅 기록" 수동 입력 폼에도 배출량 0.1g 지원
**파일**: `index.html`, `roasts.js`(`adjWt`)

이전에 고친 0.1g 조정은 **빈본 타이머 자체 화면**(iframe 안)에만 적용됐던 거였고, 로스팅 로그 탭의 "+추가"나 빈본 타이머에서 넘어와서 뜨는 **별도의 수동 입력 폼**(`moRoastForm`, `f_ro` 필드)에는 없었음 — 이게 사용자가 "아직도 안 보인다"고 한 진짜 위치. `adjWt()`를 부동소수점 안전 방식으로 바꾸고 −0.1/+0.1 버튼을 별도 줄로 추가(모바일 레이아웃 squeeze 방지, 지난번 배운 교훈 적용).

### 14. 알람음 개선 (완전 해결은 아님)
**파일**: `binbon_timer.html`, `index.html`(iframe `allow="autoplay"`)

볼륨업(0.3→0.7~1.0), 배출 완료 알람을 2회→6회 반복(2.5초에 걸쳐)으로 확장, `AudioContext`가 suspended 상태면 `resume()` 시도(화면 복귀 시에도), iframe에 `allow="autoplay"` 추가. **사용자가 "안 들려도 되니 그냥 놔둬"라고 해서 이 이상은 추가 조사 안 함** — 무음 스위치가 원인일 가능성이 높다고 안내했음.

### 15. 로스팅 시작 시 화면 스크롤 위치 버그
**파일**: `binbon_timer.html`

설정화면에서 아래로 스크롤한 상태로 "로스팅 시작"을 누르면, 화면(스크린)은 running 화면으로 바뀌지만 **스크롤 위치는 그대로 남아있어서** 맨 위(잔여시간 카운터)가 아니라 중간(기준표)이 보이는 버그. `doStart()`/`handleReset()` 끝에 `window.scrollTo(0,0)` 추가해서 해결. 로컬 테스트로 스크롤 235px→0 확인.

### 16. "로스팅 기록" 수동 입력 폼 좌우 스크롤 버그
**파일**: `style.css`

원인은 `.mode-btns{display:flex;gap:5px}`에 `flex-wrap`이 없었던 것 — 오늘 세션 초반에 모드 1·8번 버튼을 추가하면서 8개가 한 줄에 안 들어가고 넘침. `flex-wrap:wrap` 추가로 해결, `.mbox`(모달 박스)에도 `overflow-x:hidden` 방어코드 추가.

### 17. 전체 색상 테마 개편 (배경 + 폰트 4색 체계)
**파일**: `style.css`, `roasts.js`, `app.js`

#### 배경
기존 `#1e252c`(청록빛)이 마음에 안 든다고 해서 4가지 톤(중립 차콜/커피 다크브라운/완전블랙/밝은 회색)을 실제 대시보드 목업으로 만들어 비교 → **밝은 회색 톤(D안, `#40444b`)**으로 확정. 카드/보더 등 배경 계열 변수 전부 이 톤 기준으로 재조정.

#### 폰트 색상 — 최대 4색으로 통일
- **기본**: `#ffffff` (순수 흰색)
- **보조**(라벨/메타정보): `#d6cec2` (웜그레이, 처음 제안한 값보다 더 밝게 조정)
- **포인트**(강조/활성/숫자): `#ffc857` (앰버) — 기존 teal/amber/purple 전부 이 색 하나로 통합
- **경고**(재고소진/손실율 등): `#ff8a6a` (코랄오렌지) — 기존 coral 대체

`:root`의 색상 변수(`--teal`,`--amber`,`--coral`,`--purple`,`--text`,`--text2`,`--muted`,`--muted2`,`--green` 등)를 이 4색 기준으로 재정의하고, 변수를 안 쓰던 하드코딩 색상들도 전부 찾아서 교체:
- `.badge-draft`, `.auto-badge.ac-loss`(#e07b30), `.auto-badge.ac-dtr`(#5b9cf6 — **청색 계열**), `.ntag`/`.btnd`(#ff8080), `.ctag` 관련 rgba 값들, 브루잉 타이머 "완료" 상태 텍스트(#2a5a4a/#3a5a4a/#3a3020 — 배경과 거의 구분 안 되던 것)
- **`roasts.js`의 `levelColor` 매핑**(2곳): `'미디엄':'var(--teal)'`, `'미디엄-다크':'#5ec4b8'`가 CSS 변수를 안 써서 색상 개편에서 누락됐던 걸 사용자가 발견 → 전부 `var(--amber)`(다크만 `var(--coral)`)로 수정
- **`app.js`의 `ORIGIN_COLORS`**: 페루가 `#94a3b8`(블루그레이)로 하드코딩되어 있던 것을 `#c9a876`(탄색)으로 교체

#### 빈본 타이머(iframe)도 일부 반영
메인 앱과 별도 색 체계(그린/앰버/브라운)라 원래 안 건드렸는데, "1~3번(라이트) 그룹의 청록색도 밝은 노란색으로" 요청 있어서 `GROUP_COLOR`의 라이트 색상만 `#0D6E54`(청록빛 그린) → `#F0B429`(골드 옐로우)로 변경. 미디움/다크는 그대로 유지.

**검증**: 로컬 서버로 대시보드/로스팅로그/로스팅상세/생두목록/생두상세 전부 재확인, 코드 전체(`*.js`, `style.css`) grep으로 남은 파란색/청록색 계열 하드코딩 색상 없는 것 최종 확인.

## 최종 배포 버전

`app.js`의 `binbon_timer.html` 캐시버스팅: `?v=20260824h`, `index.html`의 `style.css?v=20260824a`/`app.js?v=20260824b`/`roasts.js?v=20260824b`

GitHub 최신 커밋: `8ac72b3` — "전체 색상 테마 개편: D안 배경 + 4색 폰트체계(흰색/웜그레이/앰버/코랄), 빈본타이머 라이트그룹 색상도 골드로 변경, 로스팅기록 좌우스크롤 수정"

**주의**: 이 세션에서 브라우저 테스트하다가 실수로 가짜 로스팅 기록이 Google Sheets에 잠깐 저장된 적 있음 — 즉시 발견해서 삭제 후 재동기화했고, `curl`로 Sheets 재확인해서 깨끗한 상태(가짜 데이터 없음) 확인함. 혹시 폰에서 낯선 로스팅 기록(생두명 없이 1:00/0:20 시간의)이 보이면 알려줄 것.

### 18. 폰트 크기 6단계로 단순화 + 빈본 타이머 그룹색 재조정
**파일**: `style.css`, `binbon_timer.html`, `app.js`

#### 폰트 단순화
메인 앱에 17종, 빈본 타이머(표 제외)에 12종으로 흩어져 있던 font-size를 **6단계 체계**(12/13/15/17/20/24px, 대형숫자 34/40/64/80px는 별도 유지)로 통일. 작은 텍스트일수록 상대적으로 더 크게(1~2px) 키워짐. 표(`itbl` 관련 CSS)는 요청대로 손대지 않음.

#### 빈본 타이머 그룹 색상 재배치
1~3번(라이트) 색이 너무 밝다는 피드백 → 색상을 한 칸씩 어둡게 시프트:
- 라이트: `#F0B429`(골드) → `#C77A1F` (예전 미디움 색)
- 미디움: `#C77A1F` → `#7A3B10` (예전 다크 색)
- 다크: `#7A3B10` → `#4A2408` (신규, 더 어두움)

**주의사항 발견**: 표 색상(`buildTableHtml`)이 `GROUP_COLOR["다크"]`를 참조하고 있어서, 이 변경을 그대로 적용하면 표도 같이 어두워질 뻔했음 — "표는 그대로"라는 요청에 맞춰 표 색상을 `#7A3B10` 하드코딩 고정값으로 분리함. **향후 GROUP_COLOR를 또 바꿀 일이 있으면, 표 색상은 별도로 고정돼 있다는 것 기억할 것** (buildTableHtml 맨 위 `const color="#7A3B10"` 부분).

로그 등록 화면의 DTR% 배지도 파란색(`#5b9cf6`) 하드코딩이 남아있던 걸 발견해서 `#7A3B10`(표와 동일)으로 교체.

**검증**: 모드1/4/6 선택 시 칩 색상과 표 헤더 색상을 JS로 직접 읽어서 값 확인 완료. 전체 화면 가로스크롤/콘솔에러 없음 확인.

## 최종 배포 버전

`app.js`의 `binbon_timer.html` 캐시버스팅: `?v=20260824i`, `index.html`의 `style.css?v=20260824c`

GitHub 최신 커밋: `23427e8` — "폰트 크기 6단계로 단순화(메인+빈본타이머), 빈본타이머 그룹색 어둡게 재배치(표는 고정), 로그등록 DTR배지 파란색 제거"

---

## 다음 세션에서 가능한 작업

- [ ] 커핑(`cupping.js`)의 생두 선택도 같은 원산지+한글정렬 패턴 이미 있었는지 재확인, 없으면 통일
- [ ] `BINBON_MODES_BASE_DATA.md`에 남아있던 "POP_ROWS를 DTR 선택 UI가 대체할지" 논점은 결론적으로 POP_ROWS를 그대로 유지하고 새 값으로만 교체하는 쪽으로 진행됨 — 실사용해보고 더 손볼지 확인
- [ ] 모드 1·8번은 실사용 이력이 아직 없으니, 몇 번 써본 뒤 실측 데이터로 재검증 필요
- [ ] GitHub Pages 반영 확인 (`https://jhlee810-coffee.github.io/coffee-log/`, 최대 10분 캐시 지연)
- [ ] 실기기(아이폰)에서 Wake Lock 실제 작동 확인 + 알람음이 이제 들리는지 재확인. 여전히 안 들리면 무음스위치/볼륨 문제일 가능성 높으니 그때는 소리 크기·길이 조정이나 진동(navigator.vibrate) 추가 검토
